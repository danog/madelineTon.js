class Parser
{
    constructor(objects) {
        this.objects = objects
    }
    setObjectStore(objects) {
        this.objects = objects        
    }

    deserialize(data, type) {
        
    }
}
export default Parser